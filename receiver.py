import socket
import struct
import time
import random
import json
# import some API 

# Packet header: | ChannelType (1B) | SeqNo (2B) | Timestamp (4B) | Payload |

def main():
    # packet_data = gameNetAPI.get()
    # PRINT: SeqNo, ChannelType, Timestamp, retransmissions, packet arrivals, RTT
    
    # print(f"Sequence Number: {packet_data.seq_no}")
    # print(f"Channel Type: {packet_data.channelType}")
    # print(f"Timestamp: {packet_data.timestamp}")
    # print(f"Retransmissions: {packet_data.retransmissions}")
    # print(f"RTT: {packet_data.RTT}")
    # print("_______________________")
    pass